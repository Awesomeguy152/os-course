#pragma once  // защищает файл от повторного включения при сборке

/**
 * Возвращает строку приглашения для пользовательской оболочки.
 * Строка хранится в статическом буфере и не требует освобождения.
 */
const char* vtsh_prompt();
